#= require ../underscore
# =require ../backbone
# =require ../helpers
# =require ../models/user
# =require ../models/entry

UserView = Backbone.View.extend({
  initialize: () ->
    console.log 'im a user viewer'
})

@UserView = UserView

